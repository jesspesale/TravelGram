import React from 'react';
import { SignInBtn } from '../../components';

function index(props) {
    return (
        <div className="home">
                  <SignInBtn/>
        </div>
    );
}

export default index;
